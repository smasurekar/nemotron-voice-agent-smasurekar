{"case_id": "matrix-text-in-text-out", "instructions": "Answer in one short sentence.", "text": "What is the capital of France?", "output_modalities": ["text"], "notes": "text in -> text out. No ASR, no TTS: the lowest-latency path the gateway offers."}
{"case_id": "matrix-text-in-audio-out", "instructions": "Answer in one short sentence.", "text": "What is the capital of France?", "output_modalities": ["audio"], "notes": "text in -> audio out. Skips ASR, runs TTS. This is the combination a browser RTVI session cannot do."}
{"case_id": "matrix-audio-in-text-out", "instructions": "Answer the question in one short sentence.", "audio": "question.wav", "output_modalities": ["text"], "notes": "audio in -> text out. The same question as the text cases, spoken: the answer should match."}
{"case_id": "matrix-audio-in-audio-out", "instructions": "Answer the question in one short sentence.", "audio": "question.wav", "output_modalities": ["audio"], "notes": "audio in -> audio out. The full cascaded ASR -> LLM -> TTS path, the same work an RTVI call does."}
{"case_id": "matrix-audio-in-manual-commit", "instructions": "Answer every question asked, each in one short sentence.", "audio": "question_two_part.wav", "output_modalities": ["text"], "turn_mode": "manual", "notes": "audio in -> text out with turn_detection: null. Two questions stay one turn because the client owns the commit."}
{"case_id": "matrix-text-in-tool-text-out", "instructions": "You have no weather knowledge of your own. Call get_weather for any weather question and answer only from its result, in one short sentence.", "text": "What is the weather in San Jose right now?", "output_modalities": ["text"], "tools": [{"type": "function", "name": "get_weather", "description": "Return the current weather for a city.", "parameters": {"type": "object", "properties": {"city": {"type": "string"}}, "required": ["city"], "additionalProperties": false}}], "tool_choice": "required", "tool_outputs": {"get_weather": {"city": "San Jose", "temperature_c": 21, "conditions": "sunny"}}, "notes": "text in -> tool call -> text out. Two response lifecycles on one connection."}
